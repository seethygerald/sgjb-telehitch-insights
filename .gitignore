# Telegram authentication sessions are credentials and must never be committed.
*.session
*.session-journal

# Local exports and environment secrets.
*.csv
.env
.env.*

# Python tooling.
__pycache__/
*.py[cod]
.pytest_cache/
.venv/

# AWS EC2 native Airflow deployment secrets and temporary state exports.
deploy/aws-ec2/airflow.env
deploy/aws-ec2/channel-state.json

# TypeScript incremental build metadata.
*.tsbuildinfo
